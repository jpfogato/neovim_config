// Test rust file for testing various plugin settings on neovim

fn main () {
    println!("Hello, World!");
}

struct MyStruct {
    // use this struct to test leader A-a to swap arguments
    name: str,
    surname: str,
    age: u8,
}
